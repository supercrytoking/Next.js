import styled from 'styled-components';
import { Box } from 'grommet';

export const StyledWrapper = styled(Box)`
  .verification-form {
    div {
      input {
        border-radius: 0;
      }
    }
  }
`;
